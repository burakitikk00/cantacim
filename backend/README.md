# Backend Directory

This directory is reserved for backend logic. Currently, the project uses Next.js (located in `../frontend`) for both frontend and backend (API routes).

If you plan to separate the backend into a standalone Node.js/Express/NestJS server, place it here.
